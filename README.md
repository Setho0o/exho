# exho

### Linux

ALSA is required. On Ubuntu or Debian, run this command:
```sh
sudo apt install libasound2-dev
```
All metadata and dowloading come from ytdlp so it is also required
```sh
sudo apt install yt-dlp
```


